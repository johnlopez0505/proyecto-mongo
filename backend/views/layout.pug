html 
    head
       title = pageTitle
    link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous")
    body(style="background-color:#e4ebe7")
        nav.p-0(class="container pagination" style="width: 90%; margin-top: 20px; background-color: 2a6e6b")
            ul(class="nav justify-content-center text-white" style="width: 100%;")
                li.nav-item(style="width: 14.286%;")
                    a.nav-link(href = "/auth" class="text-white btn btn-warning" data-page="/auth") Inicio
                if currentUser
                    li.nav-item(style="width: 14.286%;") 
                        a.nav-link(href = "/auth/alumnos" class="text-white" 
                            class="btn btn-warning" data-page="/auth/alumnos") Alumnos
                    li.nav-item(style="width: 14.286%;")
                        a.nav-link(href = "/auth/profesores" class="text-white" 
                            class="btn btn-warning" data-page="/auth/profesores") Profesores
                    li.nav-item(style="width: 14.286%;") 
                        a.nav-link(href = "/auth/asignaturas" class="text-white" 
                            class="btn btn-warning" data-page="/auth/asignaturas") Asignaturas
                    li.nav-item(style="width: 14.286%;")
                        a.nav-link(href = "/auth/matricular" class="text-white" 
                            class="btn btn-warning" data-page="/auth/matricular") Matriculas
                    li.nav-item(style="width: 14.286%;")
                        a.nav-link(href = "/auth/asignar-profesores" class="text-white" 
                            class="btn btn-warning" data-page="/auth/asignar-profesores") Asignar Profesores
                    li.nav-item(style="width: 14.286%;")
                        a.nav-link(href = "/auth/logout" class="text-white" 
                            class="btn btn-warning" data-page="/auth/logout") Cerrar sesión
               
                else
                    li.nav-item(style="width: 14.286%;") 
                        a.nav-link(href = "/auth/login" class="text-white" 
                            class="btn btn-warning" data-page="/auth/login") Iniciar Sesión

        script.
            document.addEventListener('DOMContentLoaded', function () {
                // Obtener la página actual 
                const currentPage = window.location.pathname;
                // obtener todos los elementos con el atributo data-page
                const navLinks = document.querySelectorAll('[data-page]');
                // Itera sobre los elementos y agrega la clase active si coincide con la página actual
                navLinks.forEach(function (link) {
                    const page = link.getAttribute('data-page');
                    if (currentPage === page) {
                        link.classList.add('active');
                    }
                });
            });

        block content 

        footer.p-0(class="container mx-auto text-center text-white" 
            style="width: 90%; margin-top: 20px; background-color: 2a6e6b;") 
            p IES Virgen del Carmen (2023)